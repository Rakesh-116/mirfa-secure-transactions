# Database connection (Postgres)
# For local dev: use PostgreSQL from Docker or local install
# For production: use Vercel Postgres, Supabase, or Neon
DATABASE_URL=postgresql://postgres:1234@localhost:5432/mirfa_db

# Master encryption key (32 bytes = 64 hex characters)
# Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
MASTER_KEY_HEX=003455e514acf9bd2e4a05d571136568711418688f669e7099aacc59d7f14b0b

# API Configuration
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# CORS (for production, replace with actual frontend URL)
CORS_ORIGIN=*
